return {
    { "nvim-tree/nvim-web-devicons", opts = {} },
    { "nvim-lua/plenary.nvim", lazy = true }
    -- These ones below are no longer needed since we load the entire spec?
    -- require("kerimbirgi.plugins.colorscheme"),
    -- require("kerimbirgi.plugins.treesitter"),
    -- require("kerimbirgi.plugins.telescope"),
    -- require("kerimbirgi.plugins.lsp")
}
